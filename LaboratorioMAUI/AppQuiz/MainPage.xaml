<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppQuiz.MainPage">

    <ScrollView>
        <VerticalStackLayout
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Spacing="15">

            <Label
                x:Name="ScoreLabel"
                Text="Punteggio: 0"
                FontSize="22"
                FontAttributes="Bold"
                HorizontalTextAlignment="Center"
                TextColor="{StaticResource Primary}"/>

            <Label
                Text="DOMANDA"
                FontSize="12"
                TextColor="Gray"
                HorizontalTextAlignment="Center"/>

            <Label 
                x:Name="QuestionTextLabel"
                Text="Caricamento domanda..."
                HorizontalTextAlignment="Center"
                FontSize="24"
                FontAttributes="Bold"
                TextColor="White"/>

            <Image
                x:Name="QuestionImage"
                WidthRequest="50"
                HeightRequest="50"/>

            <HorizontalStackLayout
                Spacing="10"
                HorizontalOptions="Center">
                
                <!-- CommandParameter: mette un parametro nel bottone -->
                <Button
                    x:Name="TrueButton"
                    Text="VERO"
                    Clicked="OnAnswer_Clicked"
                    CommandParameter="true"
                    FontSize="18"
                    TextColor="White"
                    BackgroundColor="#27ae60"
                    CornerRadius="10"
                    WidthRequest="90"/>

                <Button
                    x:Name="FalseButton"
                    Text="FALSO"
                    Clicked="OnAnswer_Clicked"
                    CommandParameter="false"
                    FontSize="18"
                    TextColor="White"
                    BackgroundColor="Red"
                    CornerRadius="10"
                    WidthRequest="90"/>

            </HorizontalStackLayout>
            <!--
            <Button
                x:Name="ResetButton"
                Text="Reset"
                FontAttributes="Bold"
                FontSize="18"
                Clicked="ResetButton_Clicked"/>-->

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
